<%= simple_form_for(@universe) do |f| %>

<%= f.input :name, placeholder: '宇宙' %>
<%= f.association :sands %>
<%= f.association :lives %>

<%= f.button :submit, 'Create', :class => 'btn-primary' %>
 
<% end %>
