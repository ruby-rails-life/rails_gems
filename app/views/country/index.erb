<h1>Countries</h1>
<%= Town::COLUMBUS_NY %>:<%= Town::COLUMBUS_NY.id %>
<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>People</th>
      <th>Cities</th>
    </tr>
  </thead>
  <tbody>
    <% @countries.each do |country| %>
      <tr>
        <td><%= country.id %></td>
        <td><%= country.name %></td>
        <td>
        <% country.people.each do |person| %>
          <%= person.name %>|<%= person.gender %><br> 
        <% end %>
        </td>
        <td>
        <% country.cities.each do |city| %>
          <%= city.name %>|<%= city.street %><br> 
        <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>