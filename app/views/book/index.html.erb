<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <div class="row">
        <div class="col-md-12">
          <h3>Test Class Method in Module : <%= Book.say_today %></h3>
        </div>
      </div>
      <div class="row">
        <div class="col-md-12">  
          <table class="table table-striped">
            <caption><strong>All Books</strong></caption>
            <thead>
              <tr>
                <th>id</th>
                <th>title</th>
                <th>author</th>
              </tr>
            </thead>
            <tbody>
              <% @books.each do |book| %>
              <tr>
                <td><%= book.id %></td>
                <td><%= book.title %></td>
                <td><%= book.show_name? %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
    <div class="row">
        <div class="col-md-12">  
          <table class="table table-striped"">
            <caption><strong>Published Books</strong></caption>
            <thead>
              <tr>
                <th>id</th>
                <th>title</th>
                <th>author</th>
              </tr>
            </thead>
            <tbody>
              <% @books_published.each do |book| %>
              <tr>
                <td><%= book.id %></td>
                <td><%= book.title %></td>
                <td><%= book.show_name? %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
    <div class="row">
        <div class="col-md-12">  
          <table class="table table-striped"">
            <caption><strong>Order by state books</strong></caption>
            <thead>
              <tr>
                <th>id</th>
                <th>title</th>
                <th>author</th>
              </tr>
            </thead>
            <tbody>
              <% @books_order.each do |book| %>
              <tr>
                <td><%= book.id %></td>
                <td><%= book.title %></td>
                <td><%= book.show_name? %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>    
</div>